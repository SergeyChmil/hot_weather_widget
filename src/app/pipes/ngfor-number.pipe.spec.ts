import { NgforNumberPipe } from './ngfor-number.pipe';

describe('NgforNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new NgforNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
