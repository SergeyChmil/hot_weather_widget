import { ImagePathModifierPipe } from './image-path-modifier.pipe';

describe('ImagePathModifierPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePathModifierPipe();
    expect(pipe).toBeTruthy();
  });
});
